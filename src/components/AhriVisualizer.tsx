import React from 'react';
import { HolographicCore, HolographicCoreProps } from './HolographicCore';

export const AhriVisualizer: React.FC<HolographicCoreProps> = (props) => {
  return <HolographicCore {...props} />;
};

export default AhriVisualizer;
